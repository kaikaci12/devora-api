### Node
node_modules/

### Environment / secrets
.env
.env.local
.env.*.local
secrets/
secrets/**

### Build output
dist/
build/
*.tsbuildinfo

### Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

### OS files
.DS_Store
Thumbs.db

### Editors
.vscode/
.idea/

### Dependency directories
/.pnp
.pnp.js

### Lockfiles (optional)
# If you use a lockfile, keep it. If not, ignore it.
package-lock.json
yarn.lock
pnpm-lock.yaml

### Runtime
npm-debug.log*
coverage/

### TypeScript
*.tsbuildinfo

### Misc
.env.test
.swp
.swo

# If you accidentally committed sensitive files (like .env or secrets/),
# remove them from Git history after updating .gitignore with:
# git rm --cached .env
# git rm -r --cached secrets/
# git commit -m "Remove secrets from repository"
# (For full history purge use the BFG or git-filter-repo tool.)